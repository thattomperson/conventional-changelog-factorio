{{#if scope}} {{scope}}:
{{~/if}}{{#if subject}}
  {{~subject}}
{{~else}}
  {{~header}}
{{~/if}}
  ({{~shortHash}})
{{~!-- commit references --}}
{{~#if references~}}
  , closes
  {{~#each references}}
    {{~#if this.owner}}
      {{~this.owner}}/
    {{~/if}}
    {{~this.repository}}{{this.prefix}}{{this.issue}}
  {{/each}}
{{~/if}}

